# CyberTao: Dice Beast Protocol - 美术美化推进策略

**制定日期**: 2026-03-30
**基于版本**: v0.1.44
**制定依据**: 全部 UI/渲染文件代码审计 + `Demo_Roadmap_2p5D_zh.md` 第四/五阶段目标

> 说明：本文已完成一轮保守编码修复，主体内容仍可能残留少量历史损坏字符 `?`。
> 若后续继续执行美化任务，请同时参考最新 `changelog_v0.1.md`，不要只按本文版本号判断项目状态。

---
## 1. 当前视觉状态评?

### 1.1 现状总结

项目当前处于**纯代码绘制阶?*——所有视觉元素由 `_draw()` 调用和代码生?UI 构成，零图片资源、零精灵、零粒子、零 Shader?

| 模块 | 当前实现 | 视觉层级 |
|------|----------|----------|
| 棋盘?| `draw_rect` 交替色块 + 2px 线框 | 原型?|
| 地形?| 颜色填充 + 文字标签（HIGH/TRAP/遭遇/回复/商店/宝箱?| 原型?|
| 单位 | 46x46 纯色矩形（青=玩家，红=敌方? 2字缩写名 | 原型?|
| 高亮 | 半透明矩形覆盖（青/?紫） | 原型?|
| 攻击反馈 | 白色闪烁 + 浮动伤害数字 | 基础?|
| 棋盘背景 | 全屏暗色 ColorRect | 原型?|
| 卡牌战斗面板 | CyberStyle 面板 + 文字按钮 | 基础?|
| 奖励/升级面板 | CyberStyle 面板 + 文字按钮 | 基础?|
| 牌组面板 | RichTextLabel 列表 | 基础?|
| HUD | 纯标?+ CyberStyle 按钮 | 基础?|
| 掷骰演出 | 无（直接更新数字?| 缺失 |
| 召唤演出 | 无（直接出现?| 缺失 |
| BGM/音效 | ?| 缺失 |

### 1.2 已有优势

- **CyberStyle 体系完善**：统一的赛博朋克配色（17种颜色常量）、面板工厂、按钮工厂已就绪，新增视觉只需在此基础上扩?
- **Tween 动画基础**：已有浮动数字、闪烁反馈的 Tween 模式，可复用
- **架构清晰**：渲染（BoardView）和逻辑（BFC）分离，美化不影响游戏逻辑

### 1.3 约束条件

- **渲染?*: `gl_compatibility`（不支持 Vulkan-only Shader 特性，但支?CanvasItem Shader?
- **引擎**: Godot 4.6.1（支?CPUParticles2D、CanvasItem Shader、Tween、AnimationPlayer?
- **布局**: 全部绝对像素定位?280x720 设计分辨率）
- **BoardView ?648 ?*：禁止继续堆积渲染逻辑，需剥离新增视觉到独立模?

---

## 2. 美化优先级分析（投入产出比排序）

基于"对玩家第一印象影响?× 实现成本的倒数"排序?

| 优先?| 美化?| 视觉冲击?| 工作?| 技术风?|
|--------|--------|-----------|--------|----------|
| P0 | 棋盘格视觉升级（发光边缘+渐变填充+格子图标?| ★★★★?| ?| ?|
| P0 | 单位视觉升级（独特形?发光轮廓+idle微动画） | ★★★★?| ?| ?|
| P1 | 掷骰演出（骰子旋转动?crest图标掉落?| ★★★★ | ?| ?|
| P1 | 攻击/技能演出增强（粒子+震屏+拖尾?| ★★★★ | ?| ?|
| P2 | 卡牌战斗面板重设计（卡牌样式+能量?HP条） | ★★?| ?| ?|
| P2 | 背景氛围升级（动态网?粒子+渐变?| ★★?| ?| ?|
| P3 | 召唤展开演出（路径铺?单位出场?| ★★?| ?| ?|
| P3 | UI 过渡动画（面板弹?关闭+按钮脉冲?| ★★ | ?| ?|
| P4 | 音效系统接入（基础音效?| ★★?| ?| ?|
| P4 | 2.5D 棋盘倾斜透视 | ★★★★?| ?| ?|

---

## 3. 分阶段推进计?

### Phase 1：格子与单位视觉升级（P0 ?最高优先）

**目标**：棋盘从"调试原型"变为"有风格辨识度的游戏画?

**子任?1.1 ?棋盘格视觉升?*

?`_draw_board()` 从纯色块升级为赛博朋克风格格子：

```
当前：纯色交替矩?+ 细线?
目标?
  - 格子内部渐变填充（中心暗→边缘微亮）
  - 发光边缘线（?draw_line 叠加亮色半透明线条模拟辉光?
  - 不同格子类型使用独特图标符号（替代纯文字标签?
    - HIGH ??三角图标 + 金色发光
    - TRAP ??十字图标 + 红色脉冲
    - 遭遇 ??闪电图标 + 橙色呼吸
    - BOSS ?💀 骷髅图标 + 深红脉冲 + 粗发光框
    - 回复 ??十字图标 + 蓝白发光
    - 商店 ??菱形图标 + 青绿发光
    - 宝箱 ??六角图标 + 金色发光
    - 事件 ?? 保持但加?发光
  - 格子坐标微标注（角落半透明小字，调试用可选）
```

**技术方?*?
- 新建 `Scripts/UI/BoardCellRenderer.gd`（class_name 注册?
- 提供 `static func draw_cell(canvas: CanvasItem, pos: Vector2, size: Vector2, cell_type: String, time: float)` 系列方法
- BoardView._draw() 中调?BoardCellRenderer 的静态方法替代现?draw_rect
- 呼吸/脉冲效果通过 `Time.get_ticks_msec()` 驱动 sin() 周期变化实现，无需 Tween

**子任?1.2 ?单位视觉升级**

将单位从纯色矩形升级为可辨识的角色表现：

```
当前?6x46 纯色矩形 + 2字名称缩?+ HP文字
目标?
  - 玩家单位：圆角矩?+ 发光轮廓 + 单位类型独特图标
    - 刀盾犬 ?盾形图标（■+边框发光?
    - 黑客??菱形图标（◆+边框发光?
    - 鸦术??三角图标（▼+边框发光?
  - 敌方单位：暗红底 + 锯齿边框暗示威胁
  - HP 条（替代纯数字）：格子下方薄条，绿→黄→红渐?
  - 选中状态：金色脉冲边框（替代静态金色线框）
  - idle 微动画：选中单位轻微上下浮动?-2px，sin波）
```

**技术方?*?
- 新建 `Scripts/UI/UnitRenderer.gd`（class_name 注册?
- 提供 `static func draw_unit(canvas, pos, unit_data, is_selected, time)` 等方?
- HP 条用 `draw_rect` 双层（底?当前HP比例填充?
- 选中脉冲?`sin(time * 3.0) * 0.15 + 0.85` 控制 alpha
- idle 浮动?`sin(time * 2.0) * 2.0` 控制 y offset

**子任?1.3 ?高亮系统升级**

```
当前：半透明矩形覆盖
目标?
  - 移动高亮：青色角标（四角 L 形线条）+ 内部微弱扫描?
  - 攻击高亮：红色十字准?+ 脉冲边框
  - 召唤高亮：紫色圆环标?
```

**预估规模**：~200?BoardCellRenderer + ~150?UnitRenderer + ~80?BoardView 修改
**风险**：BoardView 修改量需控制，核心是用新 Renderer 替换?draw 调用，不新增逻辑

---

### Phase 2：动态演出系统（P1?

**目标**：关键操作有"感觉"——掷骰有期待感、攻击有冲击?

**子任?2.1 ?掷骰演出**

```
当前：点击掷??crest 数字直接更新
目标?
  - 掷骰按钮按下后：
    1. 骰子区域显示旋转/翻滚动画?D 骰面序列帧或程序化旋转方块）
    2. 落定?crest 图标逐个弹出（scale 0?.2?.0 + 发光闪烁?
    3. 6?crest 用独特颜?图标区分
  - 持续时间：~1.2秒（可调?
```

**技术方?*?
- 新建 `Scripts/UI/DiceRollAnimation.gd`
- ?DiceDebugPanel 中嵌入，掷骰时播?
- 使用 Tween 链实现序列动?
- 6?crest 图标?`draw_*` 程序化绘制（避免依赖图片资源?

**子任?2.2 ?攻击演出增强**

```
当前：白色闪?+ 浮动数字
目标?
  - 攻击命中?
    1. 屏幕微震（BoardView 整体 position 偏移 2-4px，快速回弹）
    2. 命中格粒子爆裂（CPUParticles2D?-8个小方块四散?
    3. 伤害数字弹出增强（先放大后缩?颜色渐变?
  - 击杀时：更大粒子爆裂 + 闪光持续更久
  - 敌方攻击预警：目标格红色脉冲 + 方向箭头指示
```

**技术方?*?
- 新建 `Scripts/UI/BattleEffects.gd`（管理粒子和屏幕效果?
- 屏幕震动：Tween 控制 BoardView.position
- 粒子：CPUParticles2D（gl_compatibility 兼容，不依赖 GPU 粒子?
- 粒子节点动态创?自动释放

**预估规模**：~120?DiceRollAnimation + ~160?BattleEffects + ~50行接入修?
**风险**：低，纯添加式，不修改现有逻辑

---

### Phase 3：卡牌战斗面板重设计（P2?

**目标**：卡牌战斗从"文字列表"变为"有卡牌感的界?

**子任?3.1 ?卡牌样式重设?*

```
当前?05x48 文字按钮
目标?
  - 每张手牌是一?卡牌"外观?5x120竖向）：
    - 顶部：卡牌名?
    - 中部：类型图标（程序化绘制：??十字/闪电等）
    - 底部：费?+ 数?
    - 边框颜色按类型区?
    - 升级牌发光边?
  - 手牌排列：底部扇形展开（可选，先做水平排列?
  - 出牌动画：卡牌飞向敌?自身（Tween position 动画?
  - 不可用牌：灰?锁定图标
```

**子任?3.2 ?HP?+ 能量条可视化**

```
当前：纯文字 "敌方 HP? / 8" "能量? / 3"
目标?
  - 敌方 HP 条：面板顶部横条，红色填充，受伤时闪?
  - 玩家 HP 条：面板底部横条，绿色填?
  - 能量显示：用发光圆点代替文字（已?可用分明暗）
  - 敌方意图图标化（?攻击，盾=防御，💀=重击?
```

**预估规模**：~200?CardRenderer.gd + ~100?CardBattlePanel 修改
**风险**：CardBattlePanel 需较多修改，但不影?CardBattleController 逻辑

---

### Phase 4：氛围与细节（P2-P3?

**子任?4.1 ?背景氛围**

```
当前：全屏纯?ColorRect
目标?
  - 深色渐变背景（顶暗→底微亮）
  - 棋盘区域下方半透明网格线（模拟赛博空间感）
  - 缓慢移动的微粒子（CPUParticles2D，小光点漂浮?
  - 棋盘边缘发光?
```

**子任?4.2 ?UI 过渡动画**

```
当前：面?visible = true/false 直切
目标?
  - 面板弹出：scale 0.9?.0 + alpha 0??.2秒）
  - 面板关闭：scale 1.0?.95 + alpha 1??.15秒）
  - 按钮悬停：微放大（scale 1.02? 边框亮度提升
```

**子任?4.3 ?召唤展开演出**

```
当前：路径格和召唤单位直接出?
目标?
  - 路径格逐格铺展动画（从召唤者位置向外扩散，每格 0.1秒延迟）
  - 召唤单位出场：从小到?+ 发光闪烁
```

**预估规模**：~80行背?+ ~60行过渡动?+ ~80行召唤演?
**风险**：低

---

### Phase 5：音效系统（P4?

**子任?5.1 ?基础音效接入**

```
需要的音效类型（可用免费素材或程序化生成）?
  - 掷骰：骰子翻?落定
  - 移动：轻响步?
  - 攻击命中：金属撞?
  - 受伤：电子干扰音
  - 击杀：爆?消散
  - 卡牌出牌：刷卡音
  - 回复：上升电子音
  - 遭遇触发：警报音
  - 胜利：短旋律
  - 失败：低沉电子音
  - UI 按钮点击：轻点击?
```

**技术方?*?
- 新建 `Scripts/System/AudioManager.gd`（class_name 注册，单例模式）
- 使用 AudioStreamPlayer / AudioStreamPlayer2D
- 音频资源?`Project/Audio/` 目录
- 各模块通过 AudioManager.play_sfx("attack_hit") 调用

**预估规模**：~100?AudioManager + 各模块接?~50?
**依赖**：需要音频素材文?

---

### Phase 6?.5D 棋盘（P4 ?长期目标?

> 此阶段工作量最大、风险最高，建议?Phase 1-4 完成且稳定后再启动?

**方案 A：等距伪 3D（推荐）**

```
- 棋盘从正方形网格变为等距菱形网格
- 格子?厚度"（侧面阴影）模拟立体?
- 单位立板式显示（2D 精灵 + 阴影投影?
- 摄像头固定倾斜角度（无需真正3D?
```

**方案 B：倾斜透视（备选）**

```
- 使用 Transform2D 对整个棋盘区域进行透视变换
- 近大远小效果
- 单位仍为 2D 但带缩放修正
```

**预估规模**：~500行以上，涉及 BoardView 大幅重写
**风险**：中高——需要重新计算所有点击坐标映?

---

## 4. 推荐执行顺序

```
Phase 1（格?单位视觉?    ?建议立即开始，影响最?
  ├── 1.1 棋盘格视觉升?
  ├── 1.2 单位视觉升级
  └── 1.3 高亮系统升级

Phase 2（动态演出）           ?Phase 1 完成?
  ├── 2.1 掷骰演出
  └── 2.2 攻击演出增强

Phase 3（卡牌面板重设计?    ?可与 Phase 2 交替进行
  ├── 3.1 卡牌样式重设?
  └── 3.2 HP?能量条可视化

Phase 4（氛围细节）           ?Phase 1-3 完成?
  ├── 4.1 背景氛围
  ├── 4.2 UI 过渡动画
  └── 4.3 召唤展开演出

Phase 5（音效）               ?依赖素材，可并行准备
  └── 5.1 基础音效接入

Phase 6?.5D?              ?长期目标，视 Demo 需求决?
```

---

## 5. 架构约束与技术规?

### 5.1 新增文件规划

| 文件 | 职责 | 预估行数 |
|------|------|----------|
| `Scripts/UI/BoardCellRenderer.gd` | 格子渲染（class_name 注册，静态方法） | ~200 |
| `Scripts/UI/UnitRenderer.gd` | 单位渲染（class_name 注册，静态方法） | ~150 |
| `Scripts/UI/BattleEffects.gd` | 战斗特效（粒?震屏/命中效果?| ~160 |
| `Scripts/UI/DiceRollAnimation.gd` | 掷骰动画控件 | ~120 |
| `Scripts/UI/CardRenderer.gd` | 卡牌样式渲染（可选，Phase 3?| ~200 |
| `Scripts/System/AudioManager.gd` | 音效管理器（Phase 5?| ~100 |

### 5.2 硬规?

- **不修?BoardView 的逻辑代码**（点击处理、信号等），仅替?`_draw` 中的渲染调用
- **?Renderer 全部?class_name 注册 + 静态方?*，与 CyberStyle 同模?
- **所有颜色必须走 CyberStyle 常量**（如需新增颜色，先扩展 CyberStyle?
- **CPUParticles2D 替代 GPUParticles2D**（gl_compatibility 兼容性）
- **动画时长需可配?*（常量定义在 Renderer 顶部?
- **Phase 1-4 不依赖外部图片资?*（全部程序化绘制，保持零依赖的纯净度）
- **Phase 5 音效可用外部资源**，放 `Project/Audio/` 目录

### 5.3 瘦身策略

Phase 1 完成后，BoardView 应从 648 行下降（?draw 方法删除，被 Renderer 静态调用替代）。目标是 BoardView 回到 500 行以下?

---

## 6. 每个 Phase 的完成标?

| Phase | 完成标准 |
|-------|----------|
| Phase 1 | 棋盘截图看起来像"游戏"而非"调试工具"；单位可区分类型；格子类型一目了?|
| Phase 2 | 掷骰有期待感?1秒演出）；攻击命中有冲击感（屏幕微震+粒子?|
| Phase 3 | 手牌看起来像"卡牌"；HP/能量一眼可?|
| Phase 4 | 画面有氛围感；面板出入有动感 |
| Phase 5 | 关键操作有音效反?|
| Phase 6 | 棋盘有立体感和空间感 |

---

## 7. 风险与注意事?

1. **BoardView 行数控制**：每次修改必须统计行数，超过 700 行必须先剥离
2. **性能**：CPUParticles2D 不要超过 3 个同时活跃实例；`_draw()` 中避免每帧创建新对象
3. **gl_compatibility Shader 限制**：CanvasItem Shader 可用但功能受限，复杂后处理（?bloom）不可用
4. **呼吸/脉冲动画**：统一使用 `Time.get_ticks_msec()` 驱动，不要为每个格子创建独立 Tween
5. **Phase 6 (2.5D) 需?Codex 复审**：涉及坐标系变更?BoardView 大幅重写，执行前需确认方案


